# 更新日志 / Changelog

> 版本号规则：`A.B.C`（A=大版本 / B=中版本 / C=小版本）；**tag 名与版本号同名，无 `v` 前缀**。
> 本项目的初始版本为 **1.0.1**。

---

## 1.0.1 — 2026-09-21

首个版本：项目骨架 + 天目湖校区接口侦察 + 只读采集/监控/图形界面 + 时钟对时与抢课引擎（演练）。

### 新增功能

- **登录与侦察**
  - `python run.py login`：弹出真实 Chromium，由使用者本人登录（**账号密码不经过脚本**），
    登录态判据为预约系统 origin 下的 `localStorage.token`（Bearer JWT）。
  - `python run.py recon`：登录 + 录制请求，产出 **HAR**（含响应体）与**脱敏 JSONL**（Cookie/Authorization 只记长度）。
  - `python run.py scrub`：HAR 脱敏，抹掉敏感头值与敏感接口的请求/响应体，并**复检**是否残留。
  - `python run.py stop`：让运行中的 login/recon **优雅收尾**（HAR 才会落盘）。
  - `python run.py logout`：删除本地会话文件。
- **只读数据能力**
  - `python run.py probe`：脚本直连自检（对照实验）+ 稳态 RTT 测量。
  - `python run.py snapshot`：采集「课程 → 实验项目 → 场次（含容量/已选/余量）」并落盘快照。
  - `python run.py watch`：低频余量监控（预热、逐项目容错、变化检测、指数退避、样本逐轮 JSONL 落盘）。
  - `python run.py watch-bg` / `watch-stop`：把采样器作为**独立进程**长跑 / 按 PID 精确停止。
  - `python run.py analyze`：把样本变成事件——退课（余量增加）、被抢（余量减少）、空位存活时间、变动排行、小时分布。
- **图形界面**
  - `python run.py gui`：PySide6 **只读工作台**（会话与 token 有效期、服务端/本地时间对照、
    实验项目树、场次表、日志区；网络请求在子线程；空状态显式说明）。
- **抢课引擎（演练）**
  - `python run.py clock`：N 次往返测出「服务端 − 本地」时钟偏移。
  - `python run.py grab`：预热连接 → 对时 → 按**服务端时刻**精确定时 → 预发射 → 按间隔重试 + 限速退避；
    每次发射落盘日志；默认 **dry-run**，`--plan-only` 只看计划。
- **文档**
  - `docs/接口逆向.md`（证据分级：实测 / 源证 / 待验）
  - `docs/答题链路.md`（实验前预习测试链路研究与当天抓取流程）
  - `docs/选课窗口操作手册.md`（窗口当天分步清单）
  - `docs/排课与放课规律.md`（采样纪律与首批样本结论）
  - `docs/使用手册.md`、`docs/交付说明.md`、本文件
- **工程与安全**
  - 运行时数据一律在仓库之外：`%LOCALAPPDATA%\PhyExpLab`（可用 `PHYEXP_HOME` 覆盖）。
  - 版本单一来源：`src/phyexp_lab/__init__.py` 的 `__version__`（`pyproject.toml` 动态读取）。
  - 提交前隐私硬闸：扫描学号/姓名/openid/user_id/org_id，**命中即中止提交**。

### 实测验证（可复核）

| 项 | 实测结果 |
| --- | --- |
| 脚本直连 API | 带 `Authorization: Bearer <jwt>` 的 `requests` GET → **200**（可用；无需浏览器上下文） |
| 稳态 RTT | 首次含 TLS 握手 **1250ms**；keep-alive 稳态**中位 11ms** |
| 时钟偏移 | 服务端 − 本地 = **+1.300s / +1.316s**（两次独立测量，7 次往返取中位，离散度 3–8ms） |
| 定时发射精度 | 首次发射计划偏差 **+0.1ms / +0.7ms / +1.7ms**；重试严格落在 +800ms 量级 |
| 数据规模 | 1 个开放学期、1 门课、**24 个实验项目、48 条场次**，容量 30/场 |
| 余量变动 | 33 轮采样（86 分钟）捕获 **6 次退课、2 次被抢**；空位存活时间首个闭合样本 **13.8 分钟** |
| 预习测试 | 计分且占总评 **20%**（`score_item.exp_preparation.weight`）；限时 **15 分钟**（`exam_duration`） |
| token 有效期 | **2 小时**（JWT `exp` 解析） |
| HAR 脱敏 | 脱敏前检出 5 处凭据、脱敏后 0 处，且逆向所需字段完整保留 |

### 未完成 / 已知边界

- **写操作（选课/退课）未验证**：本季选课窗口已关闭，`POST report-api/electives` 的请求体与响应判据
  仅到「源证」级别；在窗口内实测前，`grab --real` **直接报错**，绝不猜测参数。
  → 流程见 `docs/选课窗口操作手册.md`。
- **答题（预习测试）未抓取**：入口受时间与出勤状态门控，非实验时段点不开 → 只能实验当天抓。
  → 流程见 `docs/答题链路.md` §五。
- **排课/放课规律未成结论**：首批样本仅 86 分钟且全在晚间；放闸时刻、退课高峰、存活时间分布均**不足以定论**。
- **长跑依赖人工续期**：token 2 小时过期，长时间采样需周期性重新登录（本项目不保存账号密码）。
