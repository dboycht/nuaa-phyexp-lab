# 排课与放课规律 研究计划

> 目标系统：<https://phyexp.nuaa.edu.cn/>（天目湖校区「实验助手」）
> 本文是**研究计划 + 待填结论**，不是结论本身：结论必须由本项目自己采到的数据得出，未采到就留空。
> 接口细节见 [`接口逆向.md`](接口逆向.md)。

---

## 一、要回答的问题

| # | 问题 | 为什么重要 |
| --- | --- | --- |
| Q1 | 一学期里，物理实验的**场次是集中放还是分批放**？放闸的**具体时刻**是什么规律（学期初 / 每周固定时间 / 某个整点）？ | 决定抢课脚本要「盯哪个时间窗」 |
| Q2 | 单个场次从「放闸」到「选满」通常耗时多久？热门场次多快满？ | 决定提交策略与预发射提前量 |
| Q3 | **退课**（`schedule_status: canceled`）的规律：集中在什么时段？退课后余量多久被别人抢走？ | 决定「捡漏」监控的轮询间隔 |
| Q4 | 各实验项目的**热度排序**与容量分布（哪些项目是瓶颈） | 决定备选场次的优先级 |
| Q5 | 系统在不同时间窗的**响应延迟与限速阈值**（多快会被判定「过快点击」） | 决定最小提交间隔与退避参数 |
| Q6 | 同一实验项目的场次分布（星期几、节次、实验室 `locations`） | 数据研究的基础画像 |

## 二、数据来源（只读查询，均需登录态）

依据 [`接口逆向.md`](接口逆向.md) §3，可用 PostgREST 风格查询采集：

| 数据 | 查询形态（示意，实际以抓到的为准） |
| --- | --- |
| 我选的场次 | `rest/user2projects?select=...&user_id=eq.<uid>&semester_id=eq.<sid>&schedule_status=in.(elected,scheduled,canceled)` |
| 场次容量与人数 | `select=...,max_student_number,current_student_number`（挂 `schedules`/`projects` 外键内嵌） |
| 实验项目/课程结构 | `rest/course2projects?select=...&status=eq.tmpl_release` |
| 时段/地点字典 | `rest/periods`、`rest/locations`、`rest/dicts` |
| 学期边界 | `rest/semesters` |

## 三、采样方法（纪律优先：低频、只读、可中断）

1. **只读**：研究阶段**只发 GET/查询**，不调用任何选课/退课写接口。
2. **低频**：默认最小间隔 `>= 3s`（`monitor.MonitorConfig.min_interval_seconds`），命中限速立即指数退避；
   研究期建议 **60–300s** 一轮，仅在「预计放闸时间窗（如整点前后 5 分钟）」临时收紧到 3–10s。
3. **落盘格式**（本地，不进仓库）：每轮一条 JSONL 记录
   ```json
   {"t":"2026-09-21T18:56:47+08:00","slot_id":"...","taken":12,"capacity":20,"status":"elected","http_ms":312}
   ```
4. **可中断**：长跑监控随时 Ctrl+C，已采数据必须已落盘（不得只在内存里攒）。
5. **样本记账**：每轮采样记录轮次与失败原因（超时/限速/登录过期），方便判断数据是否可信 —— **失败轮次不得计入规律统计**。

## 四、分析维度

- **时间轴**：余量随时间的变化曲线（按场次）；标记出「放闸点」与「满员点」。
- **周期维度**：按星期几 / 节次统计放出量（若系统是分批放课）。
- **热度**：按项目统计「满员所需时间」与「退课率」。
- **竞争**：热门项目的余量「存活时间」（从出现空位到再次满员）。
- **限速画像**：记录每次被判限速的请求间隔，得出安全间隔下界。

## 五、结论（待填）

> 规则：**没有数据就不写**。每条结论必须附「样本量 + 采集日期 + 依据文件」，避免把猜测写成规律。

| 编号 | 结论 | 样本量 | 采集日期 | 依据 |
| --- | --- | --- | --- | --- |
| C1 | *（待采集）* | | | |
| C2 | *（待采集）* | | | |
| C3 | *（待采集）* | | | |

## 六、与抢课引擎的接口

研究结论直接喂给 `grabber.GrabConfig`：

| 研究产出 | 对应参数 |
| --- | --- |
| 放闸时刻（Q1） | 目标时间 `target_epoch` 的设定 |
| 系统响应延迟分布（Q5） | `pre_fire_offset_ms` |
| 限速阈值（Q5） | `min_submit_interval_ms`、`rate_limit_backoff_seconds` |
| 退课规律（Q3） | `monitor.MonitorConfig` 的轮询间隔与监控时段 |
| 热度排序（Q4） | 备选场次优先级列表 |

---

*创建于 2026-09-21；数据采集尚未开始，故第五节为空。*
