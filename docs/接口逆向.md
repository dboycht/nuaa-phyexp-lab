# 物理实验预约系统 接口逆向（天目湖校区）

> 目标系统：<https://phyexp.nuaa.edu.cn/>（页面标题「实验助手」）
> 适用入口：**天目湖校区** `/tianmuhu/wechat/login`
> 本文档证据分两级，**请勿混淆**：
> - **【实测】**= 本次（2026-09-21）真的发过请求或真的读过字节；
> - **【源证】**= 从线上前端 JS 包中读出的确定代码（强证据，但**未实跑**验证）；
> - **【待验】**= 尚未确认，实现时必须先用一次真实请求核准。

---

## 一、系统概览

| 项 | 值 |
| --- | --- |
| 前端 | Vue 2 SPA（`vux` UI 库）+ Tailwind，构建产物 `/tianmuhu/wechat/static/js/{manifest,vendor,app}.<hash>.js` |
| 微信集成 | 引入 `https://res.wx.qq.com/open/js/jweixin-1.6.0.js`；代码中有 `isWechatBrowser` 分支与 `rest/wechat/signature` 签名接口 |
| API 基址 | `https://phyexp.nuaa.edu.cn/tianmuhu/api` **【源证】** `var BASE_URL = '/tianmuhu/api'; axios.defaults.baseURL = BASE_URL;` |
| 请求行为 | `withCredentials = true`（带 Cookie）、`timeout = 5000`（**5 秒超时**，抢课设计必须考虑）、相对路径如 `rest/rpc/login`、`report-api/electives` |
| 鉴权 | **Bearer JWT**：登录后写 `localStorage.token = "Bearer <jwt>"`，请求拦截器附加 `Authorization: <localStorage.token>` |
| 数据层 | **PostgREST 风格**：`rest/<表>?select=...&col=eq.<值>&col=in.(a,b)`，支持外键内嵌 `users!user2project_student_id_fkey(code,name)`，单对象查询用 `Accept: application/vnd.pgrst.object+json` |

### 入口路径实测

| URL | 结果 |
| --- | --- |
| `https://phyexp.nuaa.edu.cn/tianmuhu/wechat/login` | **【实测】HTTP 200**，标题「实验助手」，返回 SPA 外壳（`<div id=app>`） |
| `https://phyexp.nuaa.edu.cn/` | **【实测】HTTP 200**，返回 104 字节的 `<meta http-equiv=refresh content="0;url=/landing">` |
| `https://phyexp.nuaa.edu.cn/landing` | **【实测】HTTP 412 Precondition Failed**（带浏览器 UA、完整 `Sec-Fetch-*` 头仍 412）⇒ 该路径不面向脚本客户端，**统一改走天目湖入口 + 真实浏览器** |
| `http://phyexp.nuaa.edu.cn/`（80 端口） | **【实测】HTTP 412**（同上） |
| 域名解析 | **【实测】** `phyexp.nuaa.edu.cn` / `phylab.nuaa.edu.cn` → `10.0.241.133`（校内网段）；80/443 均可达 |

> 结论：**只用 HTTPS + 浏览器上下文**（本项目用 Playwright），不要试图用纯 `requests` 硬穿。

---

## 二、鉴权链路

### 2.1 登录

```
POST {API_BASE}/rest/rpc/login
Content-Type: application/x-www-form-urlencoded
<body: code=<用户名/学号>&password=<MD5(明文密码)>
```

**【源证】** 前端登录页（`/tianmuhu/wechat/login`）模板含 `placeholder="用户名"` 与 `placeholder="密码" type="password"` 两个输入框，
提交时构造：

```js
var params = { code: user.username, password: MD5(user.password).toString() };
http.post("rest/rpc/login", params).then(function (response) {
  commit("login", response.data);
  if (response.data.role == "student" || ...) { ... }
});
```

- 密码是**前端 MD5 后**提交（非明文、非加盐，属于弱保护）；因此**脚本不需要明文密码**，只要能复用登录后的 token 即可。
- 登录页还有「记住我」：cookie `remenberMe`（值为 `用户名&密码`，**明文存 30 天**，见 `setCookie("remenberMe", ..., 60*24*30)`）→ 该 cookie 本身含凭据，**本项目绝不采集/保存它**（见 §五 脱敏纪律）。

**【源证】** 登录成功后的状态写入：

```js
state.isLogin = true; state.user = user;
localStorage.setItem("token", "Bearer ".concat(user.token));
if (location.pathname.endsWith("/wechat/login")) {
  if (user.not_safe) { /* 跳转 /changePassword 强制改密 */ }
}
```

- **登录成功判据（本项目采用）**：预约系统 origin 下 `localStorage.token` 非空且以 `Bearer ` 开头。
- 响应字段 **【待验】**：`token`（JWT）、`role`（`student`/`teacher`/…）、`not_safe`（密码强度不达标标记）、`id`、`code`、`name`（后四者由前端 `user.xxx` 使用推断）。
- ****【源证】** 请求拦截器**：所有后续请求自动带 `Authorization: <localStorage.token>`。

### 2.2 其它 rpc（已读出的完整清单）

| 接口 | 用途（据调用点） |
| --- | --- |
| `rest/rpc/login` | 账号密码登录 |
| `rest/rpc/change_password` | 改密（表单 `password=<新密码>`；`not_safe` 时强制） |
| `rest/rpc/unbind` | 解绑（配合 `logout`） |
| `rest/rpc/user_courses` | 我的课程（【待验】：也可能是 `rest/user2courses` 表查询的另一写法） |
| `rest/rpc/groupinfo` / `rest/rpc/group_u2p` | 分组信息 / 给学生分组 |
| `rest/rpc/exam` / `rest/rpc/submit_answer` / `rest/rpc/submit_paper` | 考试 / 答题 / 交卷 |
| `rest/rpc/reset_preparation` | 重置预习 |
| `rest/rpc/evaluate` | 教学评价 |
| `rest/rpc/apply_for_leave` | 请假申请 |

---

## 三、数据层（PostgREST）

### 3.1 语法（**源证**：真实抓到的查询串）

```
rest/user2projects?select=id,created_at,student:users!user2project_student_id_fkey(code,name),schedule_status,schedule_id=eq.<id>
rest/user_orgs?select=*,users(id,code,name)&users.role=eq.student&organization_id=eq.<key>
...&semester_id=eq.<id>&user_id=eq.<uid>&course_id=eq.<cid>&schedule_status=in.(elected,scheduled,free_schedule)
rest/user2projects_ex?select=*&schedule_id=eq.<id>&schedule_status=in.(elected,scheduled)&order=group_stamp,group_order,user_name
rest/course2projects?select=...&project_code=eq.<code>&organization_id=eq.<org>&status=eq.tmpl_release
```

- 等值：`col=eq.value`；取反：`neq.canceled`；集合：`in.(a,b,c)`；排序：`order=a,b`；投影：`select=a,b,c`；外键内嵌：`alias:table!fk_name(cols)`。
- **单对象**响应：请求头 `Accept: application/vnd.pgrst.object+json`（返回对象而非数组）。
- `PATCH`/`DELETE` 存在自定义网关兼容层 **【源证】**：当 URL 形如 `/xxx/...` 时会被改写为 `POST {BASE_URL}/xxx/post2rest/patch/...`；本项目用到的 `PATCH rest/...`（无前导斜杠）**不受该改写影响**，仍是 PATCH。

### 3.2 数据表（29 张，**源证**：从 bundle 抽出的 `rest/<table>` 字面量）

| 分类 | 表 |
| --- | --- |
| 组织/用户 | `users`、`user_orgs` |
| 课程/项目 | `courses`（由 `user2courses`、`course2projects` 外键引用）、`course2projects`、`projects`、`semesters` |
| 排课/选课 | `schedules`、`periods`、`locations`、`user2projects`、`user2projects_ex`、`user2courses` |
| 成绩/统计 | `project_scores`、`schedule_stats`（外键引用出现） |
| 报告/作业 | `reports`、`report_sendback_stats`、`data`、`data_templates` |
| 教学管理 | `leave_applications`、`teaching_evaluations`、`teaching_evaluation_items`、`teaching_evaluation_message`、`user_notifications` |
| 内容 | `wikis`、`notebooks`、`mooc`、`dicts`（字典）、`config`、`groupusers` |

### 3.3 选课（抢课）关键字段（**源证**）

| 字段 | 含义 |
| --- | --- |
| `schedule_status` | 选课状态，取值含 `elected`（已选）、`scheduled`（已排）、`free_schedule`（免修/自由）、`stopped`（停开）、`canceled`（已取消）、`pending`（待处理） |
| `max_student_number` | 该场次容量上限 |
| `current_student_number` | 当前已选人数 **（余量 = max − current，抢课监控的核心指标）** |
| `schedule_id` / `schedules.id` | 场次 ID（即前端所说的 lesson id） |
| `project_id` | 实验项目 ID |
| `course_id` / `course2projects.course_id` | 课程 ID |
| `semester_id` | 学期 ID（查询几乎都要带） |
| `organization_id` | 校区/组织（天目湖）ID |
| `ordernumber_of_schedule` | 选课序号（提交顺序相关，抢课时可能要求递增） |
| `group_stamp` / `group_order` | 分组戳/组内序号（分组实验用） |
| `att_status` / `att_dict.dictvalue` | 考勤状态（`att_y*` 前缀等） |
| `enableWechatAttCode` | 是否启用微信考勤码 |
| `compensationForAbsent` | 缺勤补课标记 |

### 3.4 写操作（选/退课）

| 动作 | 接口 | 证据 |
| --- | --- | --- |
| **选课** | `POST {API_BASE}/report-api/electives` | **【源证】** 调用点；另有 `report-api/electives/project`、`report-api/electives/<...>` |
| **退课 / 改状态** | `PATCH {API_BASE}/rest/user2projects?id=eq.<u2p_id>`（body 里改 `schedule_status`） | **【源证】** `.patch('rest/user2projects?id=eq.'...)` |
| 批量改状态 | `PATCH rest/user2projects?id=in.(...)` | **【源证】** |
| 关联课程 | `POST rest/user2courses` | **【源证】** |
| 教师改排课 | `PATCH rest/schedules?id=eq.<...>` | **【源证】** |

> ⚠️ **【待验】** 选课请求的**具体载荷字段**（是 `{schedule_id}` 还是 `{project_id, schedule_id, ...}`）必须靠一次真实抓包确认；
> 在确认之前，`src/phyexp_lab/api.py` 的对应方法**保持 `NotImplementedError`**，绝不猜参数。

---

## 四、复现本文档的方法（供后续会话照做）

1. 抓前端：`https://phyexp.nuaa.edu.cn/tianmuhu/wechat/login` 的 HTML 里有三个 JS 入口；
2. 下载 `manifest/vendor/app.<hash>.js`；
3. ⚠️ **不要直接 grep**：这三个文件是**压缩成单行的**，一次匹配会吐出上百 KB 的整行、把上下文撑爆。
   正确做法是**用正则抽取匹配子串**再落成小文件阅读，例如：
   - `["'](/[A-Za-z0-9_\-/\.\{\}\$:]{2,90})["']` → 路由/路径字面量
   - `rest/([a-z_][a-z0-9_]{2,40})` → 表名
   - `\.(post|patch|put|delete)\(\s*['"]([^'"]{1,140})['"]` → 写操作接口
   - `(?s).{0,120}(baseURL|axios\.create).{0,160}` → 请求基址与拦截器（**必须加 `(?s)`**，否则 `.` 不跨行、匹配为 0）
4. 交互式验证：`python run.py recon`（本人登录 + 记录 HAR）。

---

## 五、脱敏与合规纪律（本项目实现约束）

1. **不采集 `remenberMe` cookie**（它明文含用户名与密码）；`recon.py` 对 Cookie / `Authorization` / `Set-Cookie` 的值只记录长度。
2. 命中 `authserver|login|sso|cas|passport|captcha|verify|password` 的 URL，**不记录请求体与响应体**。
3. 账号密码**绝不经过脚本**：登录始终由使用者本人在弹出的浏览器窗口完成。
4. 提交类请求遵守低频 + 限速退避（默认最小间隔 800ms，见 `grabber.GrabConfig`），不做高频轰炸。
5. 仅供个人学习使用，遵守学校相关管理规定。

---

## 六、下一步待办（接口逆向）

- [ ] `rest/rpc/login` 真实响应结构（字段名与 `token` 形态）—— 用 `recon` 抓一次；
- [ ] 选课 `POST report-api/electives` 的请求体与响应判据（成功/已满/限速的文案）；
- [ ] 退课 `PATCH rest/user2projects` 的请求体与响应；
- [ ] 场次列表接口：确认「某课程 → 实验项目 → 场次（含 `max/current_student_number`）」的实际查询串与权限范围（学生 token 能读哪些表）；
- [ ] `/landing`（412 那套）与天目湖入口的关系（是否为新版、是否共用数据）；
- [ ] token 有效期实测（`python run.py status` 会打印 `exp` 解析结果）。

---

*本文档由 2026-09-21 的一次会话完成，方法可复现（见 §四）；证据分级见文首说明。*
