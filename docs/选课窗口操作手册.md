# 选课窗口开放当天 · 操作手册

> 用途：物理实验**选课窗口**开放时，按本文顺序执行，把"抢课脚本"从演练推到实战。
> 背景：本季窗口已关闭，因此 `POST report-api/electives` 的**请求体与响应判据尚未实测**，
> 引擎目前只能演练（`grab` 默认 dry-run）。本文就是补齐这最后一块拼图的流程。
> 相关文档：[`接口逆向.md`](接口逆向.md)（接口全貌）、[`答题链路.md`](答题链路.md)（预习测试）。

---

## 零、分工与时间预算

| 步骤 | 谁做 | 预计耗时 | 产出 |
| --- | --- | --- | --- |
| 0 自检 | 代理 | 2 分钟 | 会话/直连/对时全部绿灯 |
| 1 抓真实提交 | **用户**（点一次真实选课）+ 代理（开抓包） | 5 分钟 | 含写操作的 HAR + 脱敏 JSONL |
| 2 回填接口文档 | 代理 | 10 分钟 | `接口逆向.md` §3.4 写成实测 |
| 3 实现写接口 | 代理 | 15 分钟 | `api.submit_booking` + `grab --real` 可用 |
| 4 演练 | 代理（用户旁观） | 3 分钟 | dry-run 时间精度确认 |
| 5 实战抢课 | 用户决定，代理执行 | 视窗口 | 发射日志 + 结果 |

> ⚠️ **窗口通常很短**：第 1 步必须最先做（否则窗口一关就只能等下个学期）。
> 第 2–4 步不依赖窗口，可以在窗口外从容完成。

---

## 一、阶段 0：窗口开放前随时可做的自检

```powershell
cd D:\code\DeepSeekHarness\nuaa-phyexp-lab
$env:PYTHONIOENCODING='utf-8'

python run.py status        # 会话是否存在、token 有效期（实测约 2 小时）
python run.py probe         # 脚本直连是否可行（应看到带 token 的用例返回 200）
python run.py clock         # 对时（实测偏移约 +1.3s，本地慢）
python run.py snapshot      # 拉一次课程/项目/场次余量，确认能看到目标场次 id
```

**通过判据**：`probe` 里 `④ UA + Bearer token = 200`；`snapshot` 里能看到你要抢的场次 id。
**任一不通过**：先 `python run.py login` 重新登录（token 过期是常态，不是故障）。

---

## 二、阶段 1：窗口一开，**先抓一次真实提交**（窗口内、只做一次）

### 用户侧
1. 打开一个终端，运行：

   ```powershell
   python run.py recon --max-wait 900
   ```

2. 在弹出的 Chromium 里登录（学号 + 密码）。
3. **在窗口里手工完成一次真实的选课提交**（选中一个还有余量的场次，点提交/确认）。
   - 这一步是唯一能拿到写接口真实请求体的办法；
   - 只做**一次**，不要反复提交（避免把课表搞乱）。

### 收尾（重要，别直接关窗口）
4. 另开一个终端运行：

   ```powershell
   python run.py stop
   ```

   > 必须用 `stop` 触发优雅收尾，HAR 才会落盘；直接关窗口会丢 HAR（见 `ERROR.md` E6）。

5. 脱敏后再给我分析：

   ```powershell
   python run.py scrub C:\Users\micak\AppData\Local\PhyExpLab\recon\capture-<最新>.har
   ```

   `scrub` 会抹掉 Cookie / Authorization / 登录请求体（含密码 MD5）与敏感响应体，
   并**复检**是否还有残留（有残留会返回非 0）。

### 代理侧（我）
6. 从脱敏 HAR / JSONL 里提取写操作请求，确认四件事：
   - **URL 与 method**（预期 `POST {API}/report-api/electives`，需实测确认，可能是别的路径）；
   - **请求体字段**（是 `schedule_id`？还是 `{project_id, schedule_id, ordernumber...}`？是否表单编码？）；
   - **请求头**（是否需要额外的自定义头 / CSRF）；
   - **成功/失败/限速的响应文案**（这是结果判定 `Outcome` 的唯一依据）。

---

## 三、阶段 2：回填接口文档（代理）

把上面四项写进 [`接口逆向.md`](接口逆向.md) §3.4，并把证据级别从 **【源证】** 改为 **【实测】**，
同时补上 `§六 待办` 的勾选。

---

## 四、阶段 3：实现写接口（代理，唯一改动点）

引擎已经给真实提交留好了**注入点**，改动集中在一个函数：

```python
# src/phyexp_lab/api.py
def submit_booking(self, slot_id: str) -> BookingAttempt:
    # 按阶段 1 实测到的 URL / 请求体 / 判定文案实现
```

并在 CLI 侧把它注入引擎（`Grabber(submit_func=client.submit_booking)`）。
`grabber.py` 的定时、预热、间隔、退避、日志**都不需要改** —— 这正是把它留成注入点的原因。

同时补 **退课** 路径（`PATCH rest/user2projects?id=eq.<id>`）以便抢错了能撤回。

---

## 五、阶段 4：演练（窗口外即可）

```powershell
# 先只看计划，核对目标时刻与换算是否正确
python run.py grab --slot <场次id> --in 60 --plan-only

# 再跑一次真定时（dry-run，不发写请求），确认发射精度
python run.py grab --slot <场次id> --in 20 --pre-fire 50 --max-attempts 3
```

**通过判据**：计划里"本地发射时刻 = 服务端目标 − 偏移 − 预发射量"；发射偏差在 ±5ms 内
（实测曾达 +0.1ms）。

---

## 六、阶段 5：实战抢课

```powershell
# 目标：服务端时钟走到 12:00:00 时提交场次 4864（可给多个，逗号分隔）
python run.py grab --slot 4864 --at 12:00:00 --real
```

纪律（写进代码也写在这里）：

- **先对时预热**（引擎自动做）；
- **提交间隔 ≥ 800ms**，命中限速指数退避 —— **不要为了快而调小**；
- 每次发射都落盘 `%LOCALAPPDATA%\PhyExpLab\logs\grab-*.jsonl`（含偏差毫秒、HTTP 状态、服务端文案），
  **事后复盘只认这个文件**，不认记忆；
- 结果四分类：成功 / 已满 / 被拒 / 限速；**超时与非预期响应一律记 `unknown`**，绝不当成功。

---

## 七、风险与回滚

| 风险 | 处置 |
| --- | --- |
| 抢错场次 | 用退课接口撤回（阶段 3 一并实现）；**先确认撤销窗口是否还开着** |
| 触发限速 | 立刻停手，观察退避是否恢复；别硬顶 |
| token 中途过期（实测 2 小时） | 重新 `python run.py login`，再 `probe` 自检后继续 |
| 抓包丢了 HAR | 别关窗口，用 `python run.py stop`（`ERROR.md` E6） |
| 提交其实没成功但看起来成功 | **只认服务端返回文案 + 事后 `snapshot` 复核余量变化**，不认"没报错" |

---

## 八、检查清单（当天照打勾）

- [ ] `status` 显示 token 未过期
- [ ] `probe` 带 token 用例返回 **200**
- [ ] `clock` 对时完成（记住偏移值）
- [ ] `snapshot` 能看到目标场次 id 与余量
- [ ] 窗口开放后**第一时间**跑 `recon` 并完成一次真实提交
- [ ] 用 `stop` 优雅收尾（HAR 已落盘）
- [ ] `scrub` 脱敏并复检通过
- [ ] 写接口四要素（URL/method/请求体/判定文案）已实测并回填文档
- [ ] `api.submit_booking` 实现 + 注入 `Grabber`
- [ ] `grab --plan-only` 计划核对无误
- [ ] dry-run 演练发射偏差在 ±5ms 内
- [ ] 实战前再次 `clock` 对时
- [ ] 实战后核对 `logs\grab-*.jsonl` 与 `snapshot` 余量变化

---

*创建于 2026-09-21。窗口开放当天按本文执行；执行后把"实测结论"回填到本文与 `接口逆向.md`。*
